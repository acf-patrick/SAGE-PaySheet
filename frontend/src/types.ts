export type User = {
  id: string;
  name: string;
  lastName: string;
  username: string;
  role: string;
};

export type Paysheet = {
  userId: string;
  baseSalary: string;
  advanceOnSalary: string;
};
